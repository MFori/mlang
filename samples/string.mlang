val s1 = "Hello"
val s2 = String(10)

val world = "world!"
for(i in 0 until sizeOf(world)) {
    s2[i] = world[i];
}

val join = (. s1, " ", s2 .)
rm s2

println(join)
print("len = %d", len(join))
rm join